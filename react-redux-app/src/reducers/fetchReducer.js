const initialState = {
    isLoading: false,
    houses: []
}




export const fetchReducer = (state=initialState, action) => {
    switch(action.type) {


        default:
            return state
    }
}